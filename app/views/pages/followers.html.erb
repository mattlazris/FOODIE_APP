<div class="wrapper4 wrapper-img2 padding-top-50">
  <div class="row">
    <div class="col col-sm-12 col-sm-offset-0">
      <div class="div-design">
        <h2>Followers</h2>
        <div>
          <% @users.each do |user| %>
            <%= user.name %>
            <%= user.description %>
          <% end %>
        </div>
        <%= link_to 'Back', profile_path %>
      </div>
    </div>
  </div>
</div>
